# 第 4 课：常见问题解答

**⏱ 预计时间**：20 分钟（按需查阅）  
**🎯 学习目标**：解决使用过程中遇到的各种问题  
**📚 难度**：⭐ 参考手册  
**📋 前置要求**：遇到问题时查阅

---

## 📖 使用说明

本文档是问题解答手册，遇到问题时：

1. 先在目录中找到相关分类
2. 查看是否有你遇到的问题
3. 按照解决方案操作

如果找不到答案，可以在项目中提交 Issue。

---

## 📑 目录

- [启动问题](#-启动问题)
- [登录问题](#-登录问题)
- [功能问题](#-功能问题)
- [数据问题](#-数据问题)
- [进阶问题](#-进阶问题)

---

## 🚀 启动问题

### Q1: Docker 启动报错 "port already in use"

**错误信息**：
```
Error: Bind for 0.0.0.0:3000 failed: port is already allocated
```

**原因**：端口被其他程序占用

**解决方案**：

```bash
# 方法 1：关闭占用端口的程序
# Windows 查看占用端口的程序
netstat -ano | findstr :3000
# 找到 PID 后，在任务管理器中结束该进程

# 方法 2：修改端口
# 编辑 docker-compose.yml，修改端口映射
# 将 "3000:3000" 改为 "3001:3000"
```

---

### Q2: Docker 启动后访问不了页面

**现象**：浏览器显示 "无法访问此网站"

**排查步骤**：

```bash
# 1. 检查容器是否在运行
docker-compose ps

# 预期输出应该显示两个容器都是 "Up" 状态
# NAME                STATE
# backend             Up
# frontend            Up

# 2. 如果容器没有运行，查看日志
docker-compose logs backend
docker-compose logs frontend

# 3. 重新构建并启动
docker-compose down
docker-compose up -d --build
```

---

### Q3: 手动启动后端报错 "ModuleNotFoundError"

**错误信息**：
```
ModuleNotFoundError: No module named 'fastapi'
```

**原因**：依赖没有安装或虚拟环境没有激活

**解决方案**：

```bash
# 1. 确保在 backend 目录
cd backend

# 2. 激活虚拟环境
# Windows:
venv\Scripts\activate
# Mac/Linux:
source venv/bin/activate

# 3. 重新安装依赖
pip install -r requirements.txt

# 4. 启动
python run.py
```

---

### Q4: 前端启动报错 "npm ERR! code ENOENT"

**原因**：没有在正确的目录运行命令

**解决方案**：

```bash
# 确保在 frontend 目录
cd frontend

# 删除旧的依赖并重新安装
rm -rf node_modules
rm package-lock.json
npm install

# 启动
npm run dev
```

---

### Q5: Python 版本不对

**错误信息**：
```
SyntaxError: invalid syntax
```

**原因**：Python 版本低于 3.10

**解决方案**：

```bash
# 检查 Python 版本
python --version

# 如果版本低于 3.10，请安装新版本
# 下载地址：https://www.python.org/downloads/
```

---

## 🔐 登录问题

### Q6: 登录时提示 "Incorrect username or password"

**排查步骤**：

1. **检查用户名/密码是否正确**
   - 登录时可以使用用户名或邮箱
   - 密码区分大小写

2. **检查后端是否运行**
   - 访问 http://localhost:8000/health
   - 应该返回 `{"status": "healthy"}`

3. **检查数据库中是否有用户**
   ```bash
   # 如果需要重置，删除数据库文件
   # 在 backend 目录下
   rm trade_journal.db
   # 重启后端，重新注册
   ```

---

### Q7: 注册时提示 "Email already registered"

**原因**：该邮箱已被注册

**解决方案**：

- 使用其他邮箱注册
- 或者重置数据库（见 Q6）

---

### Q8: 登录后自动退出

**原因**：JWT 令牌过期（默认 30 分钟）

**解决方案**：

重新登录即可。如果希望延长过期时间：

```bash
# 编辑 backend/.env 文件
ACCESS_TOKEN_EXPIRE_MINUTES=480  # 改为 8 小时
```

---

### Q9: 忘记密码怎么办？

**当前版本**暂不支持密码重置功能。

**解决方案**：

1. 如果你是管理员，可以删除数据库重新开始
2. 如果有其他管理员，可以请管理员删除你的账户，然后重新注册

---

## ⚙️ 功能问题

### Q10: 创建交易时提示 "Not authorized"

**原因**：你没有该投资组合的访问权限

**排查**：

- 确认投资组合是你自己创建的
- 尝试先创建一个新的投资组合

---

### Q11: 平仓按钮点不了

**原因**：交易已经平仓

**说明**：

- 已平仓的交易状态为 "closed"
- 已平仓的交易不能再次平仓
- 可以通过编辑修改交易信息

---

### Q12: 截图上传失败

**排查步骤**：

1. **检查文件格式**
   - 支持：JPEG、PNG、WebP
   - 不支持：GIF、BMP、TIFF

2. **检查文件大小**
   - 建议不超过 5MB

3. **检查后端目录权限**
   ```bash
   # 确保 uploads 目录存在且可写
   mkdir -p backend/uploads/screenshots
   ```

---

### Q13: Analytics 页面显示全是 0

**原因**：没有已平仓的交易

**说明**：

- Analytics 只统计已平仓（closed）的交易
- 持仓中（open）的交易不计入统计
- 请先平仓一些交易后再查看

---

### Q14: 交易列表不显示新创建的交易

**解决方案**：

1. 刷新页面（F5）
2. 检查是否选择了正确的投资组合
3. 检查筛选条件（Open/Closed）

---

## 💾 数据问题

### Q15: 如何备份数据？

**数据库位置**：`backend/trade_journal.db`

**备份方法**：

```bash
# 复制数据库文件
cp backend/trade_journal.db backup/trade_journal_$(date +%Y%m%d).db
```

> 💡 建议定期备份，特别是在更新代码之前。

---

### Q16: 如何重置所有数据？

```bash
# 停止服务
docker-compose down

# 删除数据库
rm backend/trade_journal.db

# 删除上传的文件（可选）
rm -rf backend/uploads/screenshots/*

# 重新启动
docker-compose up -d
```

---

### Q17: 如何导出交易数据？

**当前版本**暂不支持导出功能。

**临时方案**：

1. 使用 DB Browser for SQLite 打开数据库
2. 在 trades 表上右键选择 "Export to CSV"

**未来计划**：

- 添加 CSV/Excel 导出功能

---

### Q18: 数据库文件损坏了怎么办？

**症状**：启动时报错 "database disk image is malformed"

**解决方案**：

```bash
# 1. 尝试修复
sqlite3 backend/trade_journal.db ".recover" | sqlite3 recovered.db

# 2. 如果修复成功，替换原文件
mv recovered.db backend/trade_journal.db

# 3. 如果修复失败，只能使用备份或重新开始
rm backend/trade_journal.db
```

---

## 🔧 进阶问题

### Q19: 如何修改默认端口？

**修改后端端口**：

```python
# 编辑 backend/run.py
uvicorn.run(
    "app.main:app",
    host="0.0.0.0",
    port=8001,  # 改为你想要的端口
    reload=True
)
```

**修改前端端口**：

```bash
# 启动时指定端口
npm run dev -- -p 3001
```

**使用 Docker 时**：

```yaml
# 编辑 docker-compose.yml
services:
  backend:
    ports:
      - "8001:8000"  # 左边是主机端口
  frontend:
    ports:
      - "3001:3000"  # 左边是主机端口
```

---

### Q20: 如何在生产环境部署？

请参考项目根目录的以下文档：

- `DEPLOYMENT.md` - 部署指南
- `DEPLOY-VIBE-MARKETCALLS.md` - 生产部署示例
- `docker-compose.prod.yml` - 生产环境配置

**基本步骤**：

1. 修改 `.env.production` 中的配置
2. 设置强密码的 SECRET_KEY
3. 配置 HTTPS（推荐使用 Let's Encrypt）
4. 使用生产级数据库（推荐 PostgreSQL）

---

### Q21: 如何添加新功能？

**后端添加新接口**：

1. 在 `models/` 下创建数据模型
2. 在 `schemas/` 下创建数据验证
3. 在 `crud/` 下创建数据库操作
4. 在 `routers/` 下创建 API 路由
5. 在 `main.py` 中注册路由

**前端添加新页面**：

1. 在 `app/dashboard/` 下创建新目录和 `page.tsx`
2. 在 `components/` 下创建所需组件
3. 在 `lib/api.ts` 中添加 API 调用函数

---

### Q22: 如何查看 API 文档？

访问 http://localhost:8000/docs

可以在这里：
- 查看所有 API 端点
- 测试 API 调用
- 查看请求/响应格式

---

### Q23: 如何贡献代码？

1. Fork 项目仓库
2. 创建功能分支
3. 提交代码
4. 创建 Pull Request

---

## 🆘 获取帮助

如果以上都无法解决你的问题：

1. **检查日志**
   ```bash
   # Docker 日志
   docker-compose logs -f
   
   # 或者后端控制台输出
   ```

2. **搜索已有 Issue**
   - 在 GitHub Issues 中搜索类似问题

3. **提交新 Issue**
   - 描述问题现象
   - 提供错误日志
   - 说明操作步骤

---

## 📌 常见问题速查表

| 问题 | 快速解决 |
|------|----------|
| 端口被占用 | 修改端口或关闭占用程序 |
| 登录失败 | 检查后端运行状态 |
| 数据显示 0 | 确认有已平仓交易 |
| 页面不刷新 | F5 刷新页面 |
| 截图上传失败 | 检查文件格式和大小 |
| 数据库损坏 | 使用备份或重置 |

---

> 📖 **返回目录**：[教程首页](./README.md)
