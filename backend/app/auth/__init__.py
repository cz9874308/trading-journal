"""
用户认证与授权模块

本模块实现了基于 JWT (JSON Web Token) 的用户认证系统，
提供安全的用户身份验证和访问控制功能。

核心组件
--------

- **dependencies**: FastAPI 依赖注入，用于获取当前用户
- **utils**: 认证工具函数，包括密码哈希和 JWT 操作

认证流程
--------

1. 用户通过 `/api/auth/login` 提交用户名和密码
2. 系统验证凭据后生成 JWT 令牌
3. 客户端在后续请求中携带令牌进行身份验证
4. 受保护的端点通过依赖注入验证令牌有效性

安全特性
--------

- 密码使用 bcrypt 算法进行哈希存储
- JWT 令牌包含过期时间，默认 30 分钟
- 支持管理员权限验证
"""
